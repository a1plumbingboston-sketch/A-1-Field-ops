export async function sendMail({to,subject,html,pdf,filename}){const key=process.env.RESEND_API_KEY,from=process.env.FIELDOPS_FROM_EMAIL;if(!key||!from)throw new Error('Email delivery is not configured');if(!to)throw new Error('Customer email is missing');const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({from,to:[to],reply_to:process.env.FIELDOPS_REPLY_TO||'a1plumbingboston@gmail.com',subject,html,attachments:[{filename,content:pdf.toString('base64')}]})});const j=await r.json();if(!r.ok)throw new Error(j.message||'Email delivery failed');return j;}
