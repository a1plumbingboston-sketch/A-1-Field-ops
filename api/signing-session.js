import {tokenOK,error} from '../lib/db.js';
import {session,usable} from '../lib/documents.js';
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({error:'GET only'});try{const token=req.query.token;if(!tokenOK(token))return res.status(400).json({error:'Invalid signing link'});const s=await session(token);usable(s);if(req.query.download==='pdf'){if(!s.signed_pdf)return res.status(404).json({error:'No signed PDF yet'});res.setHeader('Content-Type','application/pdf');res.setHeader('Content-Disposition','attachment; filename="A1-Signed-Document.pdf"');return res.status(200).send(Buffer.from(s.signed_pdf,'base64'));}const {signed_pdf,...visible}=s;return res.status(200).json(visible);}catch(e){return error(res,e);}}
