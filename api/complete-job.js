import {authorized,rpc,uuid,error} from '../lib/db.js';
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'POST only'});try{if(!await authorized(req))return res.status(401).json({error:'Authentication required'});const {job_id,estimate_id}=req.body||{};if(!uuid(job_id)&&!uuid(estimate_id))return res.status(400).json({error:'Invalid job or estimate'});return res.status(200).json(await rpc('fieldops_complete_job',{p_job_id:job_id||null,p_estimate_id:estimate_id||null}));}catch(e){return error(res,e);}}
